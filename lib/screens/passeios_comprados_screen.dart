import 'package:flutter/material.dart';

class PasseiosCompradosScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Passeios Comprados')),
      body: Center(child: Text('Aqui vão os passeios já comprados')),
    );
  }
}
