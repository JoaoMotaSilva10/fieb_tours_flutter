import 'package:flutter/material.dart';

class AvaliacoesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Avaliações')),
      body: Center(child: Text('Aqui vão as avaliações dos passeios')),
    );
  }
}
